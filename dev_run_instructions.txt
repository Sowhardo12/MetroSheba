http://localhost:5000/api/stations/fare?startId=1&endId=6

to test ; keep script under tests/ and name as something.test.js
inside backend: run-> npm test

entering DB: $ docker exec -it metrosheba_db psql -U user -d metrosheba


test api: $ npx autocannon -c 200 -d 20 http://localhost:5000/api/health
where 200 is number of connections
and 20 is time window


to change status of map:
http://localhost:5173/internal-system/dhaka-mrt-override-panel-6 visit here and commit

FOR project tree:
powershell go to root dir:
Get-ChildItem -Recurse | Where-Object { $_.FullName -notmatch '\\(node_modules|\.git)($|\\)' } | ForEach-Object { $depth = ($_.FullName.Replace($PWD.Path, "").Split([System.IO.Path]::DirectorySeparatorChar).Count - 1); $prefix = "  " * $depth; $icon = if ($_.PSIsContainer) { "📁 " } else { "📄 " }; $prefix + $icon + $_.Name } > project-tree.txt
